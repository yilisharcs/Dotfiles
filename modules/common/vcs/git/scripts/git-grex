#!/usr/bin/env nu

# Grep an entire git repo's history
def main [...str: string] {
        let regexp = ($str | str join " ")
        let rev_list = (git rev-list --all | lines)
        git grep -E $regexp ...$rev_list
}
