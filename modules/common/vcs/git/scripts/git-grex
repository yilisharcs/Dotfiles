#!/usr/bin/env nu

# Search git history for commits that change the number of matches for a regex
def main [
    regexp: string,    # The regex to search for
    ...args: string    # Optional arguments
] {
    ^git log --all -G $regexp -p ...$args
}
