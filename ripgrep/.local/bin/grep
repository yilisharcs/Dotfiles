#!/usr/bin/env nu

$env.config.display_errors.termination_signal = false

def --wrapped main [...rest] {
        if (is-terminal --stdout) {
                rg --vimgrep ...$rest
        } else {
                rg ...$rest
        }
}
